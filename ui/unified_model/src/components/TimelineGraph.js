import React from 'react';

const TimelineGraph = () => {
  // Visualize events/spans for selected instance (stub)
  return <div>TimelineGraph (timeline here)</div>;
};

export default TimelineGraph;
